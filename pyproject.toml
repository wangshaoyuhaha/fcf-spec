[project]
name = "financial-cognitive-framework"
version = "0.0.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.fcf]
mode = "paper-only"
authority = "docs/FCF_PROJECT_CONTROL_CENTER.md"
