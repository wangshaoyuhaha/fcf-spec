# V2-R39 D3 Deterministic Adapter

The adapter serializes an immutable R38 projection into JSON-safe registered
artifact data. Re-parsing must reproduce the original projection hash.

The adapter does not fetch, calculate, approve, or mutate evidence.

P1-P47 frozen. No P48. Operator review is mandatory.
