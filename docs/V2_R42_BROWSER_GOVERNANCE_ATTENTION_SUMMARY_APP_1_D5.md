# V2-R42 D5 Read-Only Attention Card

The existing /governance page renders the attention summary before projection
detail. It contains counts and a non-actionable warning, with no form, button,
write control, approval endpoint, or new route.

P1-P47 frozen. No P48. Operator review is mandatory.
