# V2-R37 D4 Point-in-Time Resolution

The deterministic resolver uses only checks and packets visible at the requested
as-of time. It reports missing check types and failed check types explicitly.

No missing or failed evidence can resolve as passed.

P1-P47 frozen. No P48. Operator review is mandatory.
