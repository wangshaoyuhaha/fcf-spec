# V2-R37 D3 Append-Only Validation Registry

The registry rejects duplicate check identities and prevents a candidate check
type from being overwritten. Packet checks must already be registered.

Exact seven-class coverage is required before a packet can be registered.

P1-P47 frozen. No P48. Operator review is mandatory.
