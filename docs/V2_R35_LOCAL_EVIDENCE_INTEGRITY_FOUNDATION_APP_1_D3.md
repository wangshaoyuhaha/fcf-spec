# V2-R35 D3 Append-Only Registry

The local registry rejects duplicate identifiers and hashes, discontinuous
revision numbers, predecessor mismatches, changing series identity, and inferred
records whose source hashes were not registered earlier.

The registry is immutable and does not retrieve or mutate external evidence.

P1-P47 frozen. No P48. Operator review is mandatory.
