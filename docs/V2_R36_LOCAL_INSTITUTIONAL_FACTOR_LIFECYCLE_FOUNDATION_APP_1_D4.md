# V2-R36 D4 Point-in-Time Lifecycle Resolution

The deterministic resolver uses only decisions visible at the requested as-of
time and returns the current state plus complete visible history.

Candidate expiry produces a review reason and never creates an automatic decision.

P1-P47 frozen. No P48. Operator review is mandatory.
