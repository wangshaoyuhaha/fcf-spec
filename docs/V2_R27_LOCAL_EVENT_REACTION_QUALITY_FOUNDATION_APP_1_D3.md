# V2-R27 D3 Append-Only Registry

Status: IMPLEMENTED_PENDING_VALIDATION

The registry stores immutable windows, observations, and matured quality records. Parent hashes must already be registered, duplicate identifiers and duplicate hashes are rejected, and no stored evidence is mutated or replaced.

P1-P47 frozen. No P48. Operator review required. No tag, release, or deployment.
