# FCF FCP 0085 BTC Registered Local Export Validation Runner App 1 D1-D6

Status: COMPLETED_MERGED_VALIDATED

## D1 Closed Validation Request

Define one immutable local request binding exact FCP-0022 registration and
profile contracts, canonical artifact identity, and as-of UTC.

## D2 Bounded Read-Only Path Gate

Require one existing regular non-symlink local file within the FCP-0022 byte
limit. Do not copy, rewrite, delete, cache, or disclose source bytes.

## D3 Frozen Bridge Delegation

Delegate registered-byte verification, NDJSON parsing, field mapping, clock
checks, typed observation construction, and canonicalization to FCP-0022.

## D4 Sanitized Result Projection

Emit only artifact, manifest, profile, observation-kind count, sequence-bound,
clock-bound, and non-authorizing review metadata. Exclude paths and values.

## D5 Fail-Closed Authority Boundary

Keep GAP-095 open and all provider, venue, realtime, promotion, signal,
strategy, product, wallet, account, trading, and execution authority disabled.

## D6 Validation And Closeout

Run isolated, affected BTC data, all FCP, full pytest, and all-checks suites;
restore generated outputs; audit exact files, ASCII, hashes, and
`git diff --check`; then commit, push, merge, revalidate, and synchronize.
